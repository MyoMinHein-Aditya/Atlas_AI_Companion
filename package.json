{
  "name": "atlas-root",
  "version": "0.1.0",
  "private": true,
  "description": "Root configuration and script runner for Project Atlas",
  "scripts": {
    "install:all": "npm install --prefix frontend && npm install --prefix desktop && cd backend && .\\venv\\Scripts\\pip install -r requirements.txt",
    "frontend:dev": "npm run dev --prefix frontend",
    "desktop:start": "npm start --prefix desktop",
    "dev": "powershell -Command \"Start-Process npm -ArgumentList 'run frontend:dev'; npm run desktop:start\""
  }
}
